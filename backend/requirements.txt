# =============================
# IrisArc Backend Requirements
# =============================

# --- Core Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Utilities ---
python-multipart==0.0.9        # For file uploads if used later
pydantic==2.8.2                # Data validation (FastAPI core dependency)
pydantic-settings==2.3.4       # For .env management (optional future use)
textwrap3==0.9.2               # Safer text wrapping utilities
httpx==0.27.0                  # For async requests (future API calls)

# --- CORS & Middleware ---
starlette==0.38.2              # FastAPI’s underlying ASGI toolkit
